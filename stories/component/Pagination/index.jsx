import React from 'react';
import PropTypes from 'prop-types';

export const Pagiantion = ({ user, onLogin, onLogout, onCreateAccount }) => (
  <div>

  </div>
);

Pagiantion.propTypes = {
 
};

Pagiantion.defaultProps = {
  user: null,
};
